function splitPhone (str){
  if(str.length !== 11) return
  var arr = str.split('')
  arr.splice(3,0,'-')
  arr.splice(8,0,'-')
 return arr.join('')
}
function arradd(str){
  if(str === '老师')  return true
  return false
}
module.exports={
  splitPhone:splitPhone,
  arradd:arradd
}